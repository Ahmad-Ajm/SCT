# ============================================================
# Dependencies - مكتبات Python المطلوبة
# ============================================================
# للتثبيت: pip install -r requirements.txt
# ============================================================

# === الزحف الأساسي ===
requests>=2.32.4,<3.0       # HTTP requests (sync)
aiohttp==3.10.11            # HTTP requests (async - للسرعة 5-10x)
beautifulsoup4==4.12.3      # تحليل HTML
lxml==5.3.0                 # محرّك تحليل سريع (يحتاجه bs4)
urllib3==2.2.3              # معالجة URLs
tldextract==5.1.3           # استخراج subdomain/domain/suffix
defusedxml==0.7.1           # XML parsing آمن (حماية من XXE attacks)
# brotli اختياري: الأداة لا تُعلن br في Accept-Encoding، لكن ثبّته إذا واجهت
# مواقع غير متوافقة تُرسل brotli رغم ذلك:  pip install brotli
# brotli==1.1.0

# === JavaScript Rendering ===
# يحتاج تثبيت إضافي بعد pip:
# playwright install chromium
playwright==1.47.0          # متصفح بدون واجهة لتنفيذ JS

# === معالجة البيانات ===
pandas==2.2.3               # DataFrame وتنظيم البيانات
openpyxl==3.1.5             # تصدير Excel
PyYAML==6.0.2               # قراءة config.yaml

# === الواجهة وSequelize التقدم ===
tqdm==4.66.5                # شريط تقدم في Terminal
colorama==0.4.6             # ألوان في Terminal (Windows)

# === المتغيرات والإعدادات ===
python-dotenv==1.0.1        # قراءة ملف .env للمفاتيح السرية

# === الواجهة المرئية (Web UI) ===
fastapi==0.115.5            # خادم الواجهة (API + صفحات)
uvicorn[standard]==0.32.1   # تشغيل ASGI
jinja2==3.1.4               # قوالب HTML للواجهة
python-multipart==0.0.12    # استقبال نماذج الـ form
# ملاحظة: تقرير PDF يستخدم Playwright (مذكور أعلاه) — نفّذ playwright install chromium

# === تكاملات Google APIs (اختياري - يُفعَّل عند توفّر المفاتيح) ===
google-api-python-client==2.149.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
# GA4 (سلوك المستخدم) — اختياري، ثبّته فقط إن فعّلت تكامل GA4:
# google-analytics-data==0.18.16

# === تحليل متقدم (اختياري) ===
chardet==5.2.0              # كشف encoding النص
python-dateutil==2.9.0      # معالجة التواريخ

# === التطوير (Optional) ===
# black==24.10.0            # تنسيق الكود
# mypy==1.13.0              # فحص الأنواع
# pytest==8.3.3             # الاختبارات
