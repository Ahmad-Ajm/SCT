# SCT — سجل الفحص الشامل (مُغلق)

> آخر تحديث: 2026-05-21.
> هذا الملف كان يضمّ قائمة الأخطاء والتحسينات المعلّقة بعد فحص شامل عميق.
> **تم إصلاح جميع البنود.** لم تعد هناك عناصر معلّقة. التفاصيل أدناه للأرشفة فقط؛
> للتغييرات التفصيلية راجع `CHANGELOG.md`.

## الحالة: لا توجد بنود معلّقة ✅

أُغلقت كل النتائج (3 حرجة، 7 عالية، 9 متوسطة، 8 منخفضة) وأُضيفت اختبارات انحدار
تحميها في `tests/test_core_behaviors.py`.

### ملخص ما عولِج (مؤرشف)

| المعرّف | الوصف المختصر | الحالة |
| --- | --- | --- |
| C1 | تعليق الزاحف async عند بلوغ `max_pages` | ✅ مُصلَح (إنهاء بـ gather + عدّاد busy) |
| C2 | استئناف async معطّل | ✅ مُصلَح (visited/queue في DB) |
| C3 | تكرار صفوف الجداول الفرعية عند إعادة التشغيل | ✅ مُصلَح (حذف-ثم-إدراج) |
| H1 | robots لا يُفحص على وجهات redirect | ✅ مُصلَح |
| H2 | I/O متزامن يجمّد حلقة الأحداث | ✅ مُصلَح (run_in_executor) |
| H3 | Ctrl+C لا يوقف async بأمان | ✅ مُصلَح (signal handlers + حفظ حالة) |
| H4 | SSRF (لا حماية من الشبكة الداخلية) | ✅ مُصلَح (is_safe_remote_url) |
| H5 | قنبلة فك ضغط gzip للـ sitemap | ✅ مُصلَح (سقف حجم تدريجي) |
| H6 | microdata schemas لا تُفحَص | ✅ مُصلَح (raw_data/properties) |
| H7 | sitemap_diff يستقبل قائمة ناقصة | ✅ مُصلَح (تراكمية + حفظ في DB) |
| M1 | حقن صيغ CSV/Excel | ✅ مُصلَح (neutralize_formula) |
| M2 | normalize_url لا يحل `..` / trailing slash | ✅ مُصلَح |
| M3 | is_internal_url يزيل www من أي موضع | ✅ مُصلَح (بادئة فقط) |
| M4 | تعارض دلالات redirects بين sync/async | ✅ مُوحَّد |
| M5 | internal_redirects ميزة ميتة | ✅ مُفعّلة |
| M6 | ترتيب سلسلة redirect بطول النص | ✅ مُصلَح (تتبّع from→to) |
| M7 | commits لكل قفزة redirect → صفوف يتيمة | ✅ مُصلَح (ضمن حزمة الصفحة) |
| M8 | Excel يُفقَد بصمت (نص طويل/None) | ✅ مُصلَح |
| M9 | مولّدات get_* تفتح transaction | ✅ مُصلَح |
| L1 | matches_any_pattern substring فقط | ✅ أُضيف دعم glob |
| L2 | sync يقرأ المحتوى كاملاً قبل القص | ✅ قراءة تدريجية بسقف |
| L3 | كشف gzip ناقص للـ sitemap | ✅ مُصلَح |
| L4 | gsc_token بصلاحيات افتراضية | ✅ chmod 600 |
| L5 | تقدير عرض العربية ضعيف | ✅ مُحسّن |
| L6 | تداخل orphan most/least | ✅ مُصلَح |
| L7 | عتبات thin_content غير قابلة للضبط | ✅ من الإعدادات + نصوص ديناميكية |
| L8 | تجاوز max_pages | ✅ يُحَل ضمن C1 |
